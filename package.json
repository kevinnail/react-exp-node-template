{
  "name": "my-fullstack-template",
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack React and Express template",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start",
    "server": "cd server && npm run start:watch",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "test": "concurrently \"cd client && npm test\" \"cd server && npm test\"",
    "lint": "cd client && npm run lint && cd ../server && npm run lint",
    "format": "cd client && npm run format && cd ../server && npm run format"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "concurrently": "^9.2.0",
    "eslint": "^9.36.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^16.4.0",
    "prettier": "^3.6.2"
  }
}
